<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.demouserloginapplication1">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoUserLoginApplication1"
        tools:targetApi="31">
        <activity
            android:name=".UpdateUser"
            android:exported="false"
            android:label="@string/update_activity_name"/>
        <activity
            android:name=".ListView"
            android:exported="false"
            android:label="@string/listview_activity_name"/>
        <activity
            android:name=".AddData_DB"
            android:exported="false"
            android:label="@string/adduser_activity_name"/>
        <activity
            android:name=".RegistrationPage"
            android:exported="false"
            android:label="@string/registration_activity_name"/>
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginPage"
            android:label="@string/login_activity_name"/>
    </application>

</manifest>